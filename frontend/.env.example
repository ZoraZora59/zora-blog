# 前端请求后端 API 的基础地址
VITE_API_BASE_URL="http://localhost:3001/api"

# 七牛云 CDN 访问域名（不含协议），仅对该域名下的图片 URL 追加 imageView2 压缩/裁剪参数
# 支持逗号分隔多个域名；留空则不做 CDN 层压缩（例如 Unsplash / 本地 uploads 仍原样返回）
# VITE_QINIU_PUBLIC_HOST="cdn.example.com"
